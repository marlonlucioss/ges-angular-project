import { EmailInputDirective } from './input-pattern-validator.directive';

describe('EmailInputDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailInputDirective();
    expect(directive).toBeTruthy();
  });
});
